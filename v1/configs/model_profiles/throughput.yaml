# Profile: throughput — tối ưu RPS (Milestone 4)
# Dùng cho grid search: max_num_seqs × max_num_batched_tokens
max_model_len: 512
max_num_seqs: 128
max_num_batched_tokens: 8192
gpu_memory_utilization: 0.85
enable_chunked_prefill: true
